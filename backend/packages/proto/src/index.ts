import path from "path";

export const PROTO_DIR = path.resolve(__dirname, "../proto");

export const TRUSTEE_PROTO_PATH = path.join(PROTO_DIR, "trustee.proto");
export const INSPECTION_PROTO_PATH = path.join(PROTO_DIR, "inspection.proto");
