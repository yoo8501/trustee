apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  type: NodePort
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  selector:
    app: gateway
