apiVersion: apps/v1
kind: Deployment
metadata:
  name: trustee-service
  labels:
    app: trustee-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trustee-service
  template:
    metadata:
      labels:
        app: trustee-service
    spec:
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z mysql 3306; do
                echo "Waiting for MySQL..."
                sleep 3
              done
              echo "MySQL is ready"
      containers:
        - name: trustee-service
          image: trustee-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4001
            - name: grpc
              containerPort: 5001
          env:
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trustee-secrets
                  key: mysql-password
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trustee-secrets
                  key: rabbitmq-password
            - name: DATABASE_URL
              value: "mysql://trustee:$(MYSQL_PASSWORD)@mysql:3306/trustee_db"
            - name: RABBITMQ_URL
              value: "amqp://trustee:$(RABBITMQ_PASSWORD)@rabbitmq:5672"
            - name: HTTP_PORT
              value: "4001"
            - name: GRPC_PORT
              value: "5001"
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
