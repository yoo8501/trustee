apiVersion: v1
kind: Service
metadata:
  name: trustee-service
  labels:
    app: trustee-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4001
      targetPort: 4001
    - name: grpc
      port: 5001
      targetPort: 5001
  selector:
    app: trustee-service
