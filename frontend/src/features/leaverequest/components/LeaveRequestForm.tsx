/**
 * LeaveRequestForm — Sprint 6 핵심 휴가 신청 폼.
 * 구현 예정 (Green commit).
 */
export function LeaveRequestForm(_props: {
  holidays?: ReadonlyArray<string>;
}): React.ReactElement {
  throw new Error('LeaveRequestForm not implemented');
}
