import type { LeaveRequest } from '../schemas';

/**
 * LeaveRequestCard — /leave/my 화면의 각 신청 카드.
 * 구현 예정 (Green commit).
 */
export function LeaveRequestCard(_props: {
  request: LeaveRequest;
}): React.ReactElement {
  throw new Error('LeaveRequestCard not implemented');
}
